# Docker compose container prefix
COMPOSE_PROJECT_NAME=pterodactyl

# Data directory - relative path to the data directory
DATA_DIR=../data

# Panel domain - Set this to your panel's FQDN (without https://)
PANEL_DOMAIN=panel.example.com

# Database credentials - Generate strong passwords for both using a password generator
# Run these commands to generate secure passwords:
# For DB_PASSWORD: openssl rand -base64 32
# For DB_ROOT_PASSWORD: openssl rand -base64 32
DB_PASSWORD=
DB_ROOT_PASSWORD=

# Panel settings
# Generate HASHIDS_SALT using: openssl rand -base64 20
HASHIDS_SALT=
HASHIDS_LENGTH=8

# Generate APP_KEY using: echo "base64:$(openssl rand -base64 32)"
APP_KEY=

# Panel URL - Must match your PANEL_DOMAIN with https:// prefix
APP_URL=https://panel.example.com

# Timezone - List of valid timezones: http://php.net/manual/en/timezones.php
APP_TIMEZONE=UTC

# Service author email - Used for administrative purposes
APP_SERVICE_AUTHOR=admin@example.com

# Mail settings
# Configure these based on your SMTP server settings
MAIL_FROM=noreply@example.com
MAIL_DRIVER=smtp
MAIL_HOST=mail
MAIL_PORT=1025
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_ENCRYPTION=true 